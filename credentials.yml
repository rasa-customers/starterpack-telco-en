# This file contains the credentials for the voice & chat platforms
# which your bot is using.
# https://rasa.com/docs/rasa-pro/connectors/messaging-and-voice-channels/

# This entry is needed if you are using Rasa Enterprise. The entry represents credentials
# for the Rasa Enterprise "channel", i.e. Talk to your bot and Share with guest testers.
rasa:
  url: "http://localhost:5002/api"

# You don't need to provide anything here, this channel doesn't require any credentials.
# e.g. Can send requests to: http://<host>:<port>/webhooks/rest/webhook
# https://rasa.com/docs/reference/channels/your-own-website/#restinput
rest:
# n\a

# Websockets for real-time communication between your Rasa assistant and web applications
# e.g. http://<host>:<port>/socket.io
# https://rasa.com/docs/reference/channels/your-own-website/#socketio-channel
# socketio:
#   bot_message_evt: bot_uttered
#   session_persistence: true
#   user_message_evt: user_uttered

#facebook:
#  verify: "<verify>"
#  secret: "<your secret>"
#  page-access-token: "<your page access token>"

#slack:
#  slack_token: "<your slack token>"
#  slack_channel: "<the slack channel>"
#  slack_signing_secret: "<your slack signing secret>"

#mattermost:
#  url: "https://<mattermost instance>/api/v4"
#  token: "<bot token>"
#  webhook_url: "<callback URL>"
